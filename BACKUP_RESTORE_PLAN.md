# 备份和恢复方案

## 当前已实现的手动备份

1. 导出 CSV
   - 导出当前筛选结果
   - 默认只包含 `is_deleted = false`
   - 适合通用查看和简单备份

2. 导出 Excel
   - 导出当前筛选结果
   - 默认只包含 `is_deleted = false`
   - 文件名：`账目备份_YYYY-MM-DD.xlsx`

3. 导出全部未删除账目
   - 不受当前筛选影响
   - 只导出 `is_deleted = false`
   - 文件名：`账目完整备份_YYYY-MM-DD.xlsx`
   - 用于长期手动备份

导出字段顺序固定：

```text
日期、序号、店号、金额、类型、汇款公司、汇款方式、备注、创建时间、修改时间、操作人
```

## 恢复方式设计

### 1. 覆盖恢复

含义：

- 先把当前数据库里的账目全部隐藏或清空
- 再把备份文件里的账目导入进去

优点：

- 结果最接近备份文件
- 适合确认当前数据已经乱掉，需要回到某个备份版本

风险：

- 如果备份文件不是最新的，可能会丢失备份之后录入的数据
- 如果直接删除数据，风险很高

建议：

- 不做物理删除
- 如果以后实现覆盖恢复，也应该先把当前数据自动完整备份一份，再批量软删除旧数据，然后导入备份数据

### 2. 追加恢复

含义：

- 不动当前数据库
- 直接把备份文件里的账目作为新数据插入

优点：

- 不会破坏当前数据
- 实现简单

风险：

- 很容易产生重复账目
- 同一个 `serial_no` 可能和现有数据冲突

建议：

- 不推荐作为默认恢复方式
- 只适合导入完全不同来源的数据

### 3. 智能合并恢复

含义：

- 导入前先检查备份文件中的每一笔账
- 如果数据库里已经存在同一笔账，就跳过
- 如果不存在，再插入

推荐判重规则：

```text
store_id + serial_no
```

如果备份文件里只有店号 `store_no`，恢复时先根据 `store_no` 找到对应 `store_id`。

辅助判重规则：

```text
日期 + 店号 + 金额 + 类型 + 汇款公司 + 汇款方式 + 备注
```

辅助规则只用于提示人工检查，不建议完全自动替代 `serial_no` 判重。

优点：

- 不容易重复导入
- 不会覆盖现有数据
- 对长期使用最安全

风险：

- 实现比追加恢复复杂
- 需要恢复前展示导入预览

## 推荐方案

最安全推荐：

```text
智能合并恢复
```

推荐流程：

1. 用户选择 Excel / CSV 备份文件
2. 系统先自动导出一份当前全部未删除账目，作为恢复前备份
3. 解析备份文件
4. 根据 `store_no` 匹配店铺
5. 根据 `store_id + serial_no` 检查是否已存在
6. 显示导入预览：
   - 将新增多少条
   - 将跳过多少条重复数据
   - 有多少条店铺无法匹配
   - 有多少条格式错误
7. 用户确认后，只导入确认无重复的新账目
8. 导入完成后显示结果

## 如何避免重复数据

核心依赖：

- 数据库已有唯一约束：`transactions_store_serial_no_unique unique (store_id, serial_no)`
- 恢复前先查现有 `store_id + serial_no`
- 导入时遇到重复数据直接跳过

即使前端判断漏掉，数据库唯一约束也会兜底，防止重复写入。

## 恢复前是否自动生成当前备份

必须自动生成。

任何恢复操作开始前，都应该先生成一份当前完整备份：

```text
恢复前备份_YYYY-MM-DD_HH-mm-ss.xlsx
```

这样即使恢复文件选错、导入结果不符合预期，也可以回到恢复前状态。

## 暂不实现的内容

- 每周自动邮件备份
- 自动恢复执行页面
- 覆盖恢复
- 追加恢复
- 智能合并恢复
